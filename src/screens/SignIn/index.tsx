import React from 'react';
import * as Styled from './styles';

const SignIn: React.FC = () => {
  return (
    <Styled.Container>
      <Styled.Title>Sign in screen </Styled.Title>
    </Styled.Container>
  );
};

export default SignIn;
